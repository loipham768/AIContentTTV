import { describe, it, expect } from 'vitest'

describe('signIn with Credentials provider', () => {
  it.todo('returns session on correct credentials')
  it.todo('returns CredentialsSignin error on wrong password')
})
