---
phase: 06-ui-polish-vietnamese-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/editor/TopBar.tsx
  - components/editor/EditorClientWrapper.tsx
  - components/auth/LoginRegisterCard.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "All TopBar button labels are Vietnamese (Hoàn tác, Làm lại, Máy tính, Di động, Sao chép HTML)"
    - "TopBar app name uses text-base font-bold"
    - "Undo/Redo buttons are visually disabled with opacity-40 cursor-not-allowed when stack is empty"
    - "Clipboard failure shows a red Vietnamese error toast instead of silently failing"
    - "GrapesEditor dynamic import shows a spinner with Đang tải trình soạn thảo... while loading"
    - "Login page shows AI Content Booster heading and Vietnamese tagline above the white card"
  artifacts:
    - path: "components/editor/TopBar.tsx"
      provides: "Translated TopBar with D-01 through D-05, D-09, D-15, D-16"
    - path: "components/editor/EditorClientWrapper.tsx"
      provides: "editorInstance state wired to TopBar + canvas loading placeholder"
    - path: "components/auth/LoginRegisterCard.tsx"
      provides: "Login page with branding heading + tagline above card (D-06, D-07, D-08)"
  key_links:
    - from: "components/editor/EditorClientWrapper.tsx"
      to: "components/editor/TopBar.tsx"
      via: "editor={editorInstance} prop"
      pattern: "editor=\\{editorInstance\\}"
    - from: "components/editor/TopBar.tsx"
      to: "editor.UndoManager"
      via: "editor.on('undo redo update', cb)"
      pattern: "editor\\.on\\('undo redo"
---

<objective>
Apply all Wave 1 UI polish changes: translate TopBar button labels and tooltips to Vietnamese per D-01 through D-05, increase app name typography per D-09, add Undo/Redo disabled state tracking via GrapesJS UndoManager events per D-15, add clipboard error toast per D-16, wire editorInstance state in EditorClientWrapper with a canvas loading placeholder per D-14, and add the login page branding heading and tagline per D-06 and D-07.

Purpose: Deliver every user-facing change that does not depend on the ConfirmModal component. These are all self-contained within three existing files.
Output: Three modified files with zero new dependencies except lucide-react (already installed for PromptBar spinner).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-ui-polish-vietnamese-localization/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TopBar.tsx — translations, typography, Undo/Redo disabled state, clipboard error toast</name>
  <files>components/editor/TopBar.tsx</files>
  <action>
Replace the entire file with the following content. Key changes implement D-01, D-02, D-03, D-04, D-05, D-09, D-15, D-16.

- Add `editor?: Editor | null` prop so EditorClientWrapper can pass the mounted instance (needed for D-15 subscription).
- Add `canUndo` and `canRedo` boolean state (both default false).
- In a useEffect that depends on `editor`: when editor is truthy, define an `update` callback that reads `editor.UndoManager.hasUndo()` and `editor.UndoManager.hasRedo()` and sets both state values. Call `update()` immediately, then call `editor.on('undo redo update', update)`. Return a cleanup that calls `editor.off('undo redo update', update)`.
- Add `copyError` boolean state (default false) alongside existing `copied`.
- In `handleCopyHtml`: in the catch block, replace the silent comment with `setCopyError(true); setTimeout(() => setCopyError(false), 3000)`.
- Button label changes (D-01, D-02, D-03): `Undo` → `Hoàn tác`, `Redo` → `Làm lại`, `Desktop` → `Máy tính`, `Mobile` → `Di động`, `Copy HTML` → `Sao chép HTML`.
- Title attribute changes (D-05): `Undo (Ctrl+Z)` → `Hoàn tác (Ctrl+Z)`, `Redo (Ctrl+Y)` → `Làm lại (Ctrl+Y)`, `Desktop preview` → `Xem trước màn hình máy tính`, `Mobile preview (390px)` → `Xem trước màn hình di động (390px)`, `Copy zero-JS inline-CSS HTML to clipboard` → `Sao chép mã HTML đã chuẩn hóa CSS`.
- App name typography (D-09): change `text-sm font-semibold` to `text-base font-bold` on the AI Content Booster span.
- Undo button disabled state (D-15): add `disabled={!canUndo}` and class `${!canUndo ? 'opacity-40 cursor-not-allowed' : 'hover:bg-gray-100'}` replacing the unconditional `hover:bg-gray-100`.
- Redo button disabled state (D-15): same pattern with `canRedo`.
- Add the copyError toast below the existing copied toast: same `fixed bottom-6 left-1/2 -translate-x-1/2` position, `bg-red-600 text-white px-6 py-3 rounded-lg shadow-lg text-sm font-medium z-50`, `role="status"`, text `Không thể sao chép. Vui lòng thử lại.`.

Final file content:

```
'use client'

import { useEffect, useState } from 'react'
import type { Editor } from 'grapesjs'
import { LogoutButton } from '@/components/auth/LogoutButton'
import { isolateCss } from '@/lib/cssIsolation'

interface TopBarProps {
  editorRef: React.RefObject<Editor | null>
  editor?: Editor | null
  userEmail: string
}

export default function TopBar({ editorRef, editor, userEmail }: TopBarProps) {
  const [activeDevice, setActiveDevice] = useState<'desktop' | 'mobile'>('desktop')
  const [copied, setCopied] = useState(false)
  const [copyError, setCopyError] = useState(false)
  const [canUndo, setCanUndo] = useState(false)
  const [canRedo, setCanRedo] = useState(false)

  useEffect(() => {
    if (!editor) return
    function update() {
      setCanUndo(editor!.UndoManager.hasUndo())
      setCanRedo(editor!.UndoManager.hasRedo())
    }
    update()
    editor.on('undo redo update', update)
    return () => { editor.off('undo redo update', update) }
  }, [editor])

  async function handleCopyHtml() {
    const ed = editorRef.current
    if (!ed) return
    try {
      const html = await isolateCss(ed.getHtml(), ed.getCss() ?? '')
      await navigator.clipboard.writeText(html)
      setCopied(true)
      setTimeout(() => setCopied(false), 3000)
    } catch {
      setCopyError(true)
      setTimeout(() => setCopyError(false), 3000)
    }
  }

  return (
    <>
      <div className="flex items-center justify-between px-4 py-2 bg-white border-b border-gray-200 h-12">
        <span className="text-base font-bold text-gray-800">AI Content Booster</span>
        <div className="flex items-center gap-1">
          <button
            onClick={() => editorRef.current?.runCommand('core:undo')}
            disabled={!canUndo}
            className={`px-3 py-1 text-sm rounded text-gray-600 ${!canUndo ? 'opacity-40 cursor-not-allowed' : 'hover:bg-gray-100'}`}
            title="Hoàn tác (Ctrl+Z)"
          >
            Hoàn tác
          </button>
          <button
            onClick={() => editorRef.current?.runCommand('core:redo')}
            disabled={!canRedo}
            className={`px-3 py-1 text-sm rounded text-gray-600 ${!canRedo ? 'opacity-40 cursor-not-allowed' : 'hover:bg-gray-100'}`}
            title="Làm lại (Ctrl+Y)"
          >
            Làm lại
          </button>
          <div className="w-px h-5 bg-gray-300 mx-2" />
          <button
            onClick={() => { editorRef.current?.setDevice('Desktop'); setActiveDevice('desktop') }}
            className={`px-3 py-1 text-sm rounded ${activeDevice === 'desktop' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100'}`}
            title="Xem trước màn hình máy tính"
          >
            Máy tính
          </button>
          <button
            onClick={() => { editorRef.current?.setDevice('Mobile'); setActiveDevice('mobile') }}
            className={`px-3 py-1 text-sm rounded ${activeDevice === 'mobile' ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100'}`}
            title="Xem trước màn hình di động (390px)"
          >
            Di động
          </button>
        </div>
        <div className="flex items-center gap-3">
          <button
            onClick={handleCopyHtml}
            className="px-3 py-1 text-sm rounded bg-green-600 text-white hover:bg-green-700"
            title="Sao chép mã HTML đã chuẩn hóa CSS"
          >
            Sao chép HTML
          </button>
          <span className="text-sm text-gray-600">{userEmail}</span>
          <LogoutButton />
        </div>
      </div>
      {copied && (
        <div
          className="fixed bottom-6 left-1/2 -translate-x-1/2 bg-green-600 text-white px-6 py-3 rounded-lg shadow-lg text-sm font-medium z-50"
          role="status"
        >
          Sao chép thành công!
        </div>
      )}
      {copyError && (
        <div
          className="fixed bottom-6 left-1/2 -translate-x-1/2 bg-red-600 text-white px-6 py-3 rounded-lg shadow-lg text-sm font-medium z-50"
          role="status"
        >
          Không thể sao chép. Vui lòng thử lại.
        </div>
      )}
    </>
  )
}
```

Note: the fenced code above is the exact file content to write verbatim (without the outer fences). The variable name inside the try block is renamed from `editor` to `ed` to avoid shadowing the prop named `editor`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && echo "TSC OK"</automated>
  </verify>
  <done>
TopBar.tsx compiles without errors. Button labels are Hoàn tác/Làm lại/Máy tính/Di động/Sao chép HTML. App name uses text-base font-bold. canUndo/canRedo state exists with useEffect subscription to editor events. copyError state and red toast JSX exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite EditorClientWrapper.tsx — editorInstance state + canvas loading placeholder</name>
  <files>components/editor/EditorClientWrapper.tsx</files>
  <action>
Replace the entire file. Key changes implement D-14 (canvas loading) and wire editorInstance to TopBar for D-15.

- Add `editorInstance` state: `const [editorInstance, setEditorInstance] = useState<Editor | null>(null)`.
- Update `handleEditor`: call both `editorRef.current = editor` and `setEditorInstance(editor)`.
- Pass `editor={editorInstance}` to TopBar alongside existing props.
- Add a `loading` prop to the GrapesEditor dynamic import call. The loading component is a div with `className="flex flex-1 items-center justify-center"` containing an inner div with `className="flex flex-col items-center gap-3 text-gray-400"`. Inside: a `Loader2` icon from `lucide-react` with `className="w-8 h-8 animate-spin"` and a `<p className="text-sm">Đang tải trình soạn thảo...</p>`.
- Import `Loader2` from `lucide-react` at the top.

Final file content:

```
"use client";
import { useRef, useState } from "react";
import dynamic from "next/dynamic";
import type { Editor } from "grapesjs";
import { Loader2 } from "lucide-react";
import TopBar from "@/components/editor/TopBar";
import PromptBar from "@/components/editor/PromptBar";

const GrapesEditor = dynamic(() => import("@/components/editor/GrapesEditor"), {
  ssr: false,
  loading: () => (
    <div className="flex flex-1 items-center justify-center">
      <div className="flex flex-col items-center gap-3 text-gray-400">
        <Loader2 className="w-8 h-8 animate-spin" />
        <p className="text-sm">Đang tải trình soạn thảo...</p>
      </div>
    </div>
  ),
});
const HistoryPanel = dynamic(() => import("@/components/editor/HistoryPanel"), { ssr: false });

interface EditorClientWrapperProps { userEmail: string; }

export default function EditorClientWrapper({ userEmail }: EditorClientWrapperProps) {
  const editorRef = useRef<Editor | null>(null);
  const [historyKey, setHistoryKey] = useState(0);
  const [editorInstance, setEditorInstance] = useState<Editor | null>(null);

  function handleEditor(editor: Editor) {
    editorRef.current = editor;
    setEditorInstance(editor);
  }

  return (
    <div className="flex flex-col h-screen">
      <TopBar editorRef={editorRef} editor={editorInstance} userEmail={userEmail} />
      <div className="flex flex-1 overflow-hidden">
        <div className="flex flex-col flex-1 overflow-hidden">
          <GrapesEditor onEditor={handleEditor} />
        </div>
        <HistoryPanel editorRef={editorRef} refreshKey={historyKey} />
      </div>
      <PromptBar editorRef={editorRef} onSuccess={() => setHistoryKey(k => k + 1)} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && echo "TSC OK"</automated>
  </verify>
  <done>
EditorClientWrapper.tsx compiles. editorInstance state exists and is passed as editor prop to TopBar. GrapesEditor dynamic import has a loading prop rendering a Loader2 spinner with Đang tải trình soạn thảo... text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update LoginRegisterCard.tsx — add app name heading and tagline above card</name>
  <files>components/auth/LoginRegisterCard.tsx</files>
  <action>
The only change is the JSX return structure. The form logic, state, and validation are untouched.

Current outer JSX:
```
<div className="flex min-h-screen items-center justify-center bg-gray-50">
  <div className="bg-white rounded-lg shadow p-8 w-full max-w-md">
    {/* tabs + form */}
  </div>
</div>
```

Replace with a flex-col wrapper so the heading and tagline appear centered above the white card:
```
<div className="flex min-h-screen items-center justify-center bg-gray-50">
  <div className="flex flex-col items-center gap-6 w-full max-w-md px-4">
    <div className="text-center">
      <h1 className="text-2xl font-bold text-gray-900">AI Content Booster</h1>
      <p className="text-sm text-gray-500 mt-1">Tạo khối nội dung HTML chuẩn chất từ mô tả tiếng Việt</p>
    </div>
    <div className="bg-white rounded-lg shadow p-8 w-full">
      {/* tabs + form content unchanged */}
    </div>
  </div>
</div>
```

Write the full file with this structure. The `max-w-md` moves from the white card div to the outer flex-col container so all elements stay aligned. The white card becomes `w-full` (fills the container). Everything inside the white card — tabs, inputs, error messages, submit button — remains identical to the current file, with one additional fix: change the email input placeholder from `"you@example.com"` to `"email@example.com"` (Criterion 1 requires all user-facing placeholders to be language-neutral or Vietnamese).

Implement D-06 (heading), D-07 (tagline text), D-08 (centered above card, same color palette).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30 && echo "TSC OK"</automated>
  </verify>
  <done>
LoginRegisterCard.tsx compiles. The heading "AI Content Booster" with text-2xl font-bold text-gray-900 and the tagline with text-sm text-gray-500 appear above the white card div. All form behavior is unchanged.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors across all three modified files
2. `grep -n "Hoàn tác\|Làm lại\|Máy tính\|Di động\|Sao chép HTML" components/editor/TopBar.tsx` — all five translated labels present
3. `grep -n "text-base font-bold" components/editor/TopBar.tsx` — app name typography updated
4. `grep -n "canUndo\|canRedo\|UndoManager" components/editor/TopBar.tsx` — D-15 state and subscription present
5. `grep -n "copyError\|Không thể sao chép" components/editor/TopBar.tsx` — D-16 error toast present
6. `grep -n "editorInstance\|setEditorInstance" components/editor/EditorClientWrapper.tsx` — editorInstance state wired
7. `grep -n "Đang tải trình soạn thảo" components/editor/EditorClientWrapper.tsx` — canvas loading placeholder present
8. `grep -n "AI Content Booster\|Tạo khối nội dung" components/auth/LoginRegisterCard.tsx` — heading and tagline present
</verification>

<success_criteria>
- TopBar renders Hoàn tác, Làm lại, Máy tính, Di động, Sao chép HTML
- App name uses text-base font-bold
- Undo/Redo buttons are disabled with opacity-40 cursor-not-allowed before any edit
- Failed clipboard write shows red Vietnamese toast (not silent)
- GrapesEditor area shows centered spinner + Vietnamese text while the dynamic import resolves
- Login page shows heading and tagline centered above the white form card
- TypeScript compiles with zero errors
</success_criteria>

<output>
Create .planning/phases/06-ui-polish-vietnamese-localization/06-01-SUMMARY.md when done
</output>
