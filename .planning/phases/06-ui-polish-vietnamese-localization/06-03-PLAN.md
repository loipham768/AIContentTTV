---
phase: 06-ui-polish-vietnamese-localization
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "TypeScript compilation reports zero errors across the entire project"
    - "Next.js production build completes without errors"
    - "All four phase success criteria are visually confirmed by the developer"
  artifacts:
    - path: ".planning/phases/06-ui-polish-vietnamese-localization/06-03-SUMMARY.md"
      provides: "Verification summary with pass/fail for each success criterion"
  key_links: []
---

<objective>
Run TypeScript type-checking and a full Next.js production build to confirm the three implementation plans introduced no compilation errors. Then pause for human verification of all four phase success criteria that cannot be confirmed programmatically: Vietnamese string coverage, visual consistency, loading/error/empty state feedback, and editor layout at 1280px+.

Purpose: Gate-checks that code changes are type-safe and that the delivered UI satisfies the four measurable goals stated in the ROADMAP for Phase 6.
Output: Clean build confirmation + developer sign-off on all four success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-ui-polish-vietnamese-localization/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript and build verification</name>
  <files></files>
  <action>
Run the following two commands in sequence. Both must succeed before proceeding to the human-verify checkpoint.

Command 1 — TypeScript type check:
`npx tsc --noEmit`

Expected outcome: exits with code 0, no error output.

Command 2 — Next.js production build:
`npm run build`

Expected outcome: exits with code 0, build completes, no "Type error" or "Failed to compile" lines in output.

If either command fails: read the error output, identify which file and line caused the failure, fix the type error (without changing behavior), and re-run until both pass.

Common issues to watch for:
- TopBar's `editor` prop typed as `Editor | null | undefined` — ensure the `useEffect` null-guards with `if (!editor)` before calling `editor.UndoManager`.
- ConfirmModal `onConfirm` callback using `&&` short-circuit with a discriminated union — TypeScript may narrow correctly but verify the callback is typed as `() => void`, not `() => boolean | void`.
- HistoryPanel's `handleConfirmReopen` and `handleConfirmDelete` accepting `Project` parameter — check the `onConfirm` lambda correctly passes `modalState.project` only after narrowing.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
`npx tsc --noEmit` exits 0. `npm run build` exits 0. No compilation errors in output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all four Phase 6 success criteria</name>
  <what-built>
Wave 1 and Wave 2 plans delivered:
- Vietnamese translations across TopBar (D-01 through D-05) and login page branding (D-06, D-07)
- Undo/Redo disabled states with opacity-40 (D-15)
- Clipboard error toast in red (D-16)
- Canvas loading placeholder (D-14)
- ConfirmModal replacing window.confirm() for re-open and delete (D-10, D-11, D-12)
- HistoryPanel fetch error message (D-17)
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev`

Open http://localhost:3000 in a browser at 1280px or wider viewport width.

**Criterion 1 — All user-facing strings are Vietnamese:**
- On the login page: confirm "AI Content Booster" heading and "Tạo khối nội dung HTML chuẩn chất từ mô tả tiếng Việt" tagline appear above the white card.
- Log in and go to the editor page.
- In the TopBar, confirm button labels: "Hoàn tác", "Làm lại", "Máy tính", "Di động", "Sao chép HTML".
- Hover each button and confirm tooltips are Vietnamese (check title attributes in browser devtools if needed).

**Criterion 2 — Consistent visual style:**
- Confirm TopBar app name "AI Content Booster" appears larger and bolder than before (text-base font-bold).
- Confirm the ConfirmModal (trigger by clicking Xoá on a history item) shows a clean white card on a semi-transparent dark backdrop, with consistently rounded corners (rounded-lg) and correct button colors.

**Criterion 3 — All loading/error/empty states have explicit Vietnamese feedback:**
- On initial load, confirm the editor area briefly shows a spinner with "Đang tải trình soạn thảo..." text (may be brief on fast connections — check in Network throttling → Slow 3G if needed).
- Before making any edits, confirm Hoàn tác and Làm lại buttons are visually dimmed (opacity-40) and appear non-interactive.
- Make one edit to the canvas, then confirm Hoàn tác becomes fully opaque and clickable. After undo, confirm Làm lại becomes active.
- In the HistoryPanel sidebar, confirm the empty state message is "Chưa có nội dung nào." if no projects exist.
- If projects exist, click Xoá on one to confirm the delete ConfirmModal shows: title "Xóa khối này?", sub-text "Hành động này không thể hoàn tác.", Xóa button in red, Hủy button in gray.
- If projects exist and the canvas has unsaved edits (make a change after opening a project), click Mở on another project to confirm the re-open ConfirmModal shows: title "Thay thế nội dung hiện tại?", sub-text "Thay đổi chưa lưu sẽ bị mất.", Thay thế button in blue, Hủy button in gray.

**Criterion 4 — Editor layout coherent at 1280px+:**
- At exactly 1280px browser width, confirm: TopBar fills the full width, GrapesEditor canvas area is visible, HistoryPanel is visible on the right side (272px / w-72), PromptBar is visible at the bottom. Nothing is clipped or overflowing.

Report any criterion that fails with enough detail to reproduce it.
  </how-to-verify>
  <resume-signal>Type "approved" if all four criteria pass. Or describe which criterion failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits with code 0
2. `npm run build` exits with code 0
3. Developer confirms all four success criteria via the human-verify checkpoint
4. `grep -rn "window.confirm" components/` returns no matches — belt-and-suspenders final check
</verification>

<success_criteria>
- TypeScript type-checker reports zero errors
- Next.js production build completes without errors
- Criterion 1 verified: all user-facing English UI strings replaced with Vietnamese equivalents
- Criterion 2 verified: consistent visual style — typography, color, border-radius throughout
- Criterion 3 verified: every loading/error/empty state has explicit Vietnamese feedback message
- Criterion 4 verified: editor layout is complete and coherent at 1280px desktop viewport
</success_criteria>

<output>
Create .planning/phases/06-ui-polish-vietnamese-localization/06-03-SUMMARY.md when done
</output>
