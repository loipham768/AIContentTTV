---
phase: 01-auth-database-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - tests/unit/user-model.test.ts
  - tests/unit/mongodb.test.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "All 7 test files pass: npx vitest run exits 0 with no skips remaining in unit tests"
    - "Every User document in MongoDB has a passwordHash starting with $2b$12$ (bcryptjs, 12 rounds)"
    - "dbConnect() singleton returns the same connection object on repeated calls in the same process"
    - "npm run build exits 0 — no TypeScript errors across the entire project"
    - "A new user can register, log in, see /editor with their email, and log out — full vertical slice confirmed"
    - "Attempting to visit /editor without a session redirects to /login"
    - "Session survives browser refresh (JWT in HttpOnly cookie)"
  artifacts:
    - path: "tests/unit/user-model.test.ts"
      provides: "bcrypt hash format assertion + model guard assertion"
    - path: "tests/unit/mongodb.test.ts"
      provides: "dbConnect() singleton identity assertion"
  key_links:
    - from: "tests/unit/user-model.test.ts"
      to: "models/User.ts"
      via: "import User, bcrypt"
      pattern: "\\$2b\\$"
    - from: "tests/unit/mongodb.test.ts"
      to: "lib/mongodb.ts"
      via: "import dbConnect"
      pattern: "dbConnect"
---

<objective>
Integration + Verification (AUTH-01, AUTH-02, AUTH-03): Complete the unit test assertions for the User model and MongoDB singleton, run the full Vitest suite to confirm all tests pass, verify the bcrypt hash format in MongoDB Atlas, and perform the manual smoke test of the complete auth flow. This plan gates the phase before /gsd:verify-work runs.

Purpose: Prove that every success criterion in ROADMAP.md Phase 1 is met. No new code ships — this plan confirms what the prior plans delivered.
Output: Full Vitest suite green. bcrypt $2b$12$ format confirmed. Manual smoke test sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-auth-database-foundation/01-CONTEXT.md
@.planning/phases/01-auth-database-foundation/01-VALIDATION.md
@.planning/phases/01-auth-database-foundation/01-01-SUMMARY.md
@.planning/phases/01-auth-database-foundation/01-02-SUMMARY.md
@.planning/phases/01-auth-database-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit test assertions for User model and MongoDB singleton</name>
  <files>tests/unit/user-model.test.ts, tests/unit/mongodb.test.ts</files>
  <read_first>
    - .planning/phases/01-auth-database-foundation/01-VALIDATION.md — Per-Task Verification Map rows for user-model-hash, dbconnect-singleton
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md — Pattern 2 (model guard pattern), Pattern 1 (dbConnect singleton with global cache), Security Domain (bcrypt $2b$ prefix)
    - tests/unit/user-model.test.ts — read current stub state
    - tests/unit/mongodb.test.ts — read current stub state
    - models/User.ts — read to understand the schema fields and model guard pattern
    - lib/mongodb.ts — read to understand the global cache structure
  </read_first>
  <behavior>
    - Test (user-model): User.create with a plaintext password + manual bcrypt.hash(password, 12) produces a passwordHash starting with "$2b$12$"
    - Test (user-model): calling `mongoose.model('User', UserSchema)` twice in the same process does not throw OverwriteModelError (model guard works)
    - Test (mongodb): calling dbConnect() twice in the same process returns the same Mongoose connection object (strict object identity or same connection string)
    - Test (mongodb): dbConnect() throws if MONGODB_URI is undefined
  </behavior>
  <action>
    Replace `it.todo()` stubs in both unit test files with real assertions.

    `tests/unit/user-model.test.ts`:
    - `beforeAll`: call `dbConnect()` (requires real MONGODB_URI in environment)
    - `afterAll`: call `mongoose.connection.close()`
    - Test 1 "passwordHash starts with $2b$12$": use `bcrypt.hash('testpassword', 12)` and `expect(hash).toMatch(/^\$2b\$12\$/)`. Also create a real `User.create({ email: 'hash-test@test.com', passwordHash: hash })`, read it back with `User.findOne({ email: 'hash-test@test.com' }).lean()`, and assert `user.passwordHash.startsWith('$2b$12$')`. Delete user in `afterEach`.
    - Test 2 "model guard prevents OverwriteModelError": assert that `mongoose.models.User` is truthy after `dbConnect()` has been called (the model was registered); calling the guard expression `mongoose.models.User || mongoose.model('User', UserSchema)` does not throw. Assert `typeof mongoose.models.User === 'function'`.

    `tests/unit/mongodb.test.ts`:
    - Test 1 "returns same connection on repeated calls": call `dbConnect()` twice, store both return values, assert `conn1 === conn2` (object identity). This proves the singleton cache works.
    - Test 2 "throws if MONGODB_URI undefined": temporarily unset `process.env.MONGODB_URI`, call `dbConnect()` (importing fresh to trigger the env check), assert it throws with a message containing 'MONGODB_URI'. Restore env var in `afterEach`.

    Both files: use `beforeAll`/`afterAll` for DB connection lifecycle. Import `mongoose` from `mongoose`, `dbConnect` from `@/lib/mongodb`, `User` from `@/models/User`.
  </action>
  <verify>
    <automated>cd /c/Users/LOIPC/Desktop/managerment-projects/AIContentBooster && npx vitest run tests/unit/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/user-model.test.ts: `it(` entries replace all `it.todo()` entries — 2 real assertions
    - tests/unit/mongodb.test.ts: `it(` entries replace all `it.todo()` entries — 2 real assertions
    - `npx vitest run tests/unit/` exits 0 — all 4 unit assertions pass
    - Test assertion: `user.passwordHash.startsWith('$2b$12$')` passes (bcryptjs 12-round hash prefix)
    - Test assertion: calling dbConnect() twice returns the same connection object
  </acceptance_criteria>
  <done>Both unit test files have real assertions. All 4 unit tests pass. bcrypt $2b$12$ format confirmed by test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-01 through 01-04 collectively deliver the complete Phase 1 vertical slice:
    - Next.js 15.3.9 scaffolded with full auth infrastructure
    - MongoDB Atlas connection via dbConnect() singleton
    - User model with bcrypt-hashed passwords ($2b$12$ format)
    - NextAuth v5 JWT sessions with HttpOnly cookie
    - middleware.ts protecting /editor and /api/* routes
    - /login page with tab-toggle Login/Register form
    - POST /api/auth/register: Zod → duplicate check → bcrypt.hash(12) → User.create()
    - Login: signIn('credentials', { redirect:false }) → Vietnamese error or redirect
    - Logout: LogoutButton → signOut({ callbackUrl:'/login' })
    - Full Vitest suite: 7 test files, all assertions green
  </what-built>
  <how-to-verify>
    Run these steps in order. All must succeed before approving.

    **Step 1 — Full automated suite:**
    Run `npx vitest run` in the project root.
    Expected: exits 0, all test files green, no failures or errors.

    **Step 2 — TypeScript build:**
    Run `npm run build`.
    Expected: exits 0 with no TypeScript errors.

    **Step 3 — Development server smoke test:**
    Run `npm run dev` and open http://localhost:3000 in a browser.

    3a. Visit http://localhost:3000/editor WITHOUT logging in.
    Expected: immediate redirect to http://localhost:3000/login?callbackUrl=%2Feditor

    3b. Go to http://localhost:3000/login.
    Expected: page shows a centered card with "Login" and "Register" tabs.

    3c. Click "Register" tab. Enter email: `smoke@test.vn`, password: `testpass123`.
    Expected: form accepts input. Submit.
    Expected: redirected to http://localhost:3000/editor showing your email address.

    3d. Refresh the browser (F5 / Cmd+R).
    Expected: still on /editor showing email — session survived refresh (AUTH-02 JWT cookie).

    3e. Click "Đăng xuất" (logout button).
    Expected: redirected to http://localhost:3000/login.

    3f. Try to visit http://localhost:3000/editor again.
    Expected: redirected to /login (session cleared — AUTH-03).

    3g. Go back to /login. Click "Login" tab. Enter `smoke@test.vn` + `testpass123`.
    Expected: redirected to /editor (returning user login — AUTH-02).

    3h. Go back to /login. Click "Login" tab. Enter `smoke@test.vn` + `wrongpassword`.
    Expected: inline error "Email hoặc mật khẩu không đúng" appears. No redirect. (D-10 Vietnamese string)

    3i. Click "Register" tab. Try to register `smoke@test.vn` again.
    Expected: inline error "Email này đã được sử dụng". (D-11 Vietnamese string)

    **Step 4 — MongoDB Atlas verification:**
    Open MongoDB Atlas → Browse Collections → aicontentbooster database → users collection.
    Find the document for `smoke@test.vn`.
    Expected: `passwordHash` field starts with `$2b$12$` (bcryptjs 12-round prefix).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 4 steps pass.
    Or describe which step failed (include error message or screenshot description) so the issue can be diagnosed and fixed before marking Phase 1 complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → MongoDB Atlas | Integration tests write real data to Atlas; cleanup is required to prevent test data accumulation |
| MongoDB Atlas → Visual verification | Developer inspects the users collection to confirm bcrypt format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `tests/unit/user-model.test.ts` | mitigate | Test creates and then deletes test users in afterEach — no test data accumulates in production Atlas cluster; use a dedicated test database if `MONGODB_URI_TEST` is available |
| T-04-02 | Information Disclosure | MongoDB Atlas visual check | accept | Developer reads Atlas UI directly — no credentials exposed; Atlas connection is authenticated via MONGODB_URI in .env.local (gitignored) |
| T-04-03 | Information Disclosure | Manual smoke test | accept | smoke@test.vn is a throwaway test account; acceptable to leave in Atlas dev cluster or delete manually post-verification |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; all from Plan 01 Package Legitimacy Audit |
</threat_model>

<verification>
Phase 1 is complete when all of the following are confirmed:

**Automated (all must be green before human checkpoint):**
1. `npx vitest run` exits 0 — all 7 test files, all assertions pass
2. `npm run build` exits 0 — zero TypeScript errors

**Manual smoke test (human checkpoint):**
3. Register with new email → redirect to /editor (AUTH-01)
4. Session survives browser refresh (AUTH-02 JWT cookie)
5. Logout → redirect to /login; /editor now redirects back to /login (AUTH-03)
6. Login with correct credentials → redirect to /editor (AUTH-02 returning user)
7. Login with wrong password → "Email hoặc mật khẩu không đúng" inline error (D-10)
8. Duplicate registration → "Email này đã được sử dụng" inline error (D-11)
9. MongoDB Atlas shows $2b$12$ prefix on passwordHash field (ASVS V6, D-21)
</verification>

<success_criteria>
Phase 1 ROADMAP success criteria all met:
1. A new user can register with an email and password and immediately access the app (AUTH-01)
2. A returning user can log in and stay logged in after browser refresh without re-entering credentials (AUTH-02)
3. A logged-in user can log out from any page and is redirected to the login page (AUTH-03)
4. Attempting to register with an already-used email shows a clear error message (D-11)
5. Entering wrong credentials at login shows a Vietnamese-language error message (D-10)

Phase 1 is ready for `/gsd:verify-work 1`.
</success_criteria>

<output>
Create `.planning/phases/01-auth-database-foundation/01-04-SUMMARY.md` when done.
</output>
