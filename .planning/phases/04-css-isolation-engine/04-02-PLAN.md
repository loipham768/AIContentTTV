---
phase: 04-css-isolation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/editor/TopBar.tsx
autonomous: true
requirements:
  - EX-01
  - EX-03
must_haves:
  truths:
    - "TopBar renders a 'Copy HTML' button in the right section"
    - "Clicking 'Copy HTML' calls isolateCss(editor.getHtml(), editor.getCss()) and writes to clipboard"
    - "A 'Sao chép thành công!' toast appears after successful copy and auto-dismisses after 3 seconds"
    - "TopBar uses a React fragment wrapper (<>) to render both the bar and the toast"
    - "npx tsc --noEmit exits 0 — no TypeScript errors in TopBar.tsx"
  artifacts:
    - path: "components/editor/TopBar.tsx"
      provides: "TopBar with Copy HTML button and toast notification"
      contains: "isolateCss"
  key_links:
    - from: "components/editor/TopBar.tsx"
      to: "lib/cssIsolation.ts"
      via: "import { isolateCss } from '@/lib/cssIsolation'"
      pattern: "isolateCss"
    - from: "components/editor/TopBar.tsx"
      to: "navigator.clipboard"
      via: "navigator.clipboard.writeText(html)"
      pattern: "clipboard"
---

<objective>
Add the "Copy HTML" button and toast notification to TopBar. This wires lib/cssIsolation.ts (created in Wave 1) to the user-facing clipboard export action. After this plan, all three EX requirements are implemented.

Purpose: Single-file modification — TopBar.tsx gets a button, a state variable, an async handler, and a toast JSX node. No new files, no new routes, no database.
Output: Updated `components/editor/TopBar.tsx` — Copy HTML button functional, toast shows on success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-css-isolation-engine/04-CONTEXT.md
@.planning/phases/04-css-isolation-engine/04-01-SUMMARY.md

<interfaces>
<!-- Current TopBar.tsx — executor replaces this file exactly -->

From components/editor/TopBar.tsx (current state before this plan):
```tsx
'use client'

import type { Editor } from 'grapesjs'
import { useState } from 'react'
import { LogoutButton } from '@/components/auth/LogoutButton'

interface TopBarProps {
  editorRef: React.RefObject<Editor | null>
  userEmail: string
}

export default function TopBar({ editorRef, userEmail }: TopBarProps) {
  const [activeDevice, setActiveDevice] = useState<'desktop' | 'mobile'>('desktop')

  return (
    <div className="flex items-center justify-between px-4 py-2 bg-white border-b border-gray-200 h-12">
      {/* Left: App name */}
      <span className="text-sm font-semibold text-gray-800">AI Content Booster</span>

      {/* Center: Editor controls */}
      <div className="flex items-center gap-1">
        <button
          onClick={() => editorRef.current?.runCommand('core:undo')}
          className="px-3 py-1 text-sm rounded text-gray-600 hover:bg-gray-100"
          title="Undo (Ctrl+Z)"
        >
          Undo
        </button>
        <button
          onClick={() => editorRef.current?.runCommand('core:redo')}
          className="px-3 py-1 text-sm rounded text-gray-600 hover:bg-gray-100"
          title="Redo (Ctrl+Y)"
        >
          Redo
        </button>

        <div className="w-px h-5 bg-gray-300 mx-2" />

        <button
          onClick={() => {
            editorRef.current?.setDevice('Desktop')
            setActiveDevice('desktop')
          }}
          className={`px-3 py-1 text-sm rounded ${
            activeDevice === 'desktop'
              ? 'bg-blue-100 text-blue-700'
              : 'text-gray-600 hover:bg-gray-100'
          }`}
          title="Desktop preview"
        >
          Desktop
        </button>
        <button
          onClick={() => {
            editorRef.current?.setDevice('Mobile')
            setActiveDevice('mobile')
          }}
          className={`px-3 py-1 text-sm rounded ${
            activeDevice === 'mobile'
              ? 'bg-blue-100 text-blue-700'
              : 'text-gray-600 hover:bg-gray-100'
          }`}
          title="Mobile preview (390px)"
        >
          Mobile
        </button>
      </div>

      {/* Right: User email + logout */}
      <div className="flex items-center gap-3">
        <span className="text-sm text-gray-600">{userEmail}</span>
        <LogoutButton />
      </div>
    </div>
  )
}
```

From lib/cssIsolation.ts (created in 04-01):
```typescript
export function isolateCss(html: string, css: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TopBar.tsx with Copy HTML button and toast</name>
  <files>components/editor/TopBar.tsx</files>
  <action>
    Rewrite `components/editor/TopBar.tsx` with the following complete content. Preserve all existing behavior (undo, redo, device toggle, logout). The changes are additive:

    1. Add import: `import { isolateCss } from '@/lib/cssIsolation'`
    2. Add `copied` state: `const [copied, setCopied] = useState(false)`
    3. Add `handleCopyHtml` async function
    4. Add "Copy HTML" button to the right section (before user email)
    5. Wrap the return in a React fragment `<>…</>` and add the conditional toast after the main bar div

    Complete file content:

    ```tsx
    'use client'

    import type { Editor } from 'grapesjs'
    import { useState } from 'react'
    import { LogoutButton } from '@/components/auth/LogoutButton'
    import { isolateCss } from '@/lib/cssIsolation'

    interface TopBarProps {
      editorRef: React.RefObject<Editor | null>
      userEmail: string
    }

    export default function TopBar({ editorRef, userEmail }: TopBarProps) {
      const [activeDevice, setActiveDevice] = useState<'desktop' | 'mobile'>('desktop')
      const [copied, setCopied] = useState(false)

      async function handleCopyHtml() {
        const editor = editorRef.current
        if (!editor) return
        try {
          const html = isolateCss(editor.getHtml(), editor.getCss())
          await navigator.clipboard.writeText(html)
          setCopied(true)
          setTimeout(() => setCopied(false), 3000)
        } catch {
          // clipboard unavailable — silent in v1
        }
      }

      return (
        <>
          <div className="flex items-center justify-between px-4 py-2 bg-white border-b border-gray-200 h-12">
            {/* Left: App name */}
            <span className="text-sm font-semibold text-gray-800">AI Content Booster</span>

            {/* Center: Editor controls */}
            <div className="flex items-center gap-1">
              <button
                onClick={() => editorRef.current?.runCommand('core:undo')}
                className="px-3 py-1 text-sm rounded text-gray-600 hover:bg-gray-100"
                title="Undo (Ctrl+Z)"
              >
                Undo
              </button>
              <button
                onClick={() => editorRef.current?.runCommand('core:redo')}
                className="px-3 py-1 text-sm rounded text-gray-600 hover:bg-gray-100"
                title="Redo (Ctrl+Y)"
              >
                Redo
              </button>

              <div className="w-px h-5 bg-gray-300 mx-2" />

              <button
                onClick={() => {
                  editorRef.current?.setDevice('Desktop')
                  setActiveDevice('desktop')
                }}
                className={`px-3 py-1 text-sm rounded ${
                  activeDevice === 'desktop'
                    ? 'bg-blue-100 text-blue-700'
                    : 'text-gray-600 hover:bg-gray-100'
                }`}
                title="Desktop preview"
              >
                Desktop
              </button>
              <button
                onClick={() => {
                  editorRef.current?.setDevice('Mobile')
                  setActiveDevice('mobile')
                }}
                className={`px-3 py-1 text-sm rounded ${
                  activeDevice === 'mobile'
                    ? 'bg-blue-100 text-blue-700'
                    : 'text-gray-600 hover:bg-gray-100'
                }`}
                title="Mobile preview (390px)"
              >
                Mobile
              </button>
            </div>

            {/* Right: Copy HTML + User email + logout */}
            <div className="flex items-center gap-3">
              <button
                onClick={handleCopyHtml}
                className="px-3 py-1 text-sm rounded bg-green-600 text-white hover:bg-green-700"
                title="Copy zero-JS inline-CSS HTML to clipboard"
              >
                Copy HTML
              </button>
              <span className="text-sm text-gray-600">{userEmail}</span>
              <LogoutButton />
            </div>
          </div>

          {copied && (
            <div
              className="fixed bottom-6 left-1/2 -translate-x-1/2 bg-green-600 text-white px-6 py-3 rounded-lg shadow-lg text-sm font-medium z-50"
              role="status"
            >
              Sao chép thành công!
            </div>
          )}
        </>
      )
    }
    ```

    After editing, run `npx tsc --noEmit` to confirm zero TypeScript errors.

    Key behavioral notes for the executor:
    - `handleCopyHtml` is `async` because `navigator.clipboard.writeText()` returns a Promise
    - The try/catch is intentional — clipboard can throw if the browser hasn't granted permission or if the page is not secure context
    - `setTimeout(() => setCopied(false), 3000)` is intentionally not cleared on unmount — the component is persistent (never unmounts during a session), so this is safe
    - `role="status"` on the toast makes it an ARIA live region — screen readers announce "Sao chép thành công!" automatically
    - The React fragment `<>…</>` is required because the function must return a single root node, and the fixed toast is a sibling to the bar div
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "topbar" || echo "no TopBar errors"</automated>
  </verify>
  <done>TopBar.tsx imports isolateCss; has copied state; handleCopyHtml writes to clipboard; "Copy HTML" button in right section; toast renders conditionally; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GrapesJS canvas → handleCopyHtml | HTML/CSS from user's own editing session — trusted |
| handleCopyHtml → isolateCss | Passes raw GrapesJS strings; isolateCss applies all scrubbing (scripts, class, data-*) |
| isolateCss → clipboard | Cleaned HTML string; zero-JS guaranteed by EX-02 scrubbing in isolateCss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | HTML placed on clipboard | mitigate | isolateCss strips scripts before writeText — content on clipboard is clean regardless of GrapesJS internals |
| T-04-04 | Denial of Service | setTimeout memory leak on unmount | accept | TopBar never unmounts during a user session; risk is zero in practice |
</threat_model>

<verification>
After task completes:

1. `npx tsc --noEmit` exits 0
2. `grep -n "isolateCss" components/editor/TopBar.tsx` returns a match
3. `grep -n "handleCopyHtml" components/editor/TopBar.tsx` returns a match
4. `grep -n "navigator.clipboard" components/editor/TopBar.tsx` returns a match
5. `grep -n "Sao chép thành công" components/editor/TopBar.tsx` returns a match
6. `grep -n "setCopied" components/editor/TopBar.tsx` returns at least 2 matches (set true + set false)
7. `grep -n "Copy HTML" components/editor/TopBar.tsx` returns a match (button label)
</verification>

<success_criteria>
- TopBar.tsx: imports isolateCss; copied state + handleCopyHtml; "Copy HTML" button in right section; toast with "Sao chép thành công!" renders when copied=true; TypeScript clean
- No new files — single file modification
- No regressions to existing Undo/Redo/Device toggle behavior
</success_criteria>

<output>
Create `.planning/phases/04-css-isolation-engine/04-02-SUMMARY.md` when done
</output>
